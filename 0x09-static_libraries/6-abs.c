#include "main.h"

/**
 * _abs - will convert the input to abslute value
 * @n: the input for my function
 * Return: 1 if it's greater than 0,
 *         0 if it's equal to 0
 *        -1 if it smaller than 0
 *
*/
int _abs(int n)
{

if (n > 0)
return (n);
else
return (-n);

}
